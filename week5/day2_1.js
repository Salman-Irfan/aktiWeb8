let name1 = `anas`
console.log(`${name1} ko dollar ki smj nhi ai`) // anas

const markAbsent = (studentName) => {
    return (`${studentName} is absent`)
}

let koi =  markAbsent(`bilal`)
console.log(koi)